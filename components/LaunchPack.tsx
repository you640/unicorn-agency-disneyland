import React from 'react';
import InvestorOnePager from './InvestorOnePager';

const LaunchPack: React.FC = () => {
    return (
        // The id is used for anchor linking from the hero button
        <div id="launch-pack">
            <InvestorOnePager />
        </div>
    );
};

export default LaunchPack;
